(ns aoc.day-X
  (:require [clojure.java.io :as io]
            [clojure.string :as string]))

(def input (slurp (io/resource "aoc/day_X.txt")))

(defn solve [input]
  "TODO")

(comment
  (solve input))
